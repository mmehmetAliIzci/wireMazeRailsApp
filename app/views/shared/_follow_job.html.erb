<%= form_for(JobUserRelationship.create, remote: true) do |f| %>
	
	<%= hidden_field_tag :job_id, job.id %>
	<%= hidden_field_tag :user_id, user.id %>

	<%= f.submit "Show Interest", class: "s-btn s-outlined s-btn-red s-text-right modalbasvur" %>
<% end %>