<%= form_for(current_user.active_relationships.find_by(followed_id: user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Interested", class: "s-btn s-outlined  s-text-right modalbasvur" %>
<% end %>